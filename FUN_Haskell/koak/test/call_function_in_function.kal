def max(y : int x : int) : int
    if x > y then
        x
    else
        y;

def min(y : int x : int) : int
    if x < y then
        x
    else
        y;

def diff(x : int y : int) : int
    if x != y then
        1
    else
        0;

!(diff(max(5, 6), min(6, 7)));